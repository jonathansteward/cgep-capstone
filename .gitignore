# Terraform
.terraform/
# .terraform.lock.hcl is committed (dependency pinning) — see terraform/ and bootstrap/
*.tfstate
*.tfstate.*
*.tfvars
!terraform.tfvars.example
tfplan
*.tfplan
crash.log

# Lambda build
terraform/lambda/handler.zip

# Local
evidence/
.DS_Store
